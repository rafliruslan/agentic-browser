{
  "name": "agentic-browser-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Slack Socket Mode bridge: mention an agent, it drives your real browser via Claude Code",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@slack/bolt": "^4.2.0"
  }
}
